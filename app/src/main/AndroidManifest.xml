<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.animations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".TransitionAnimationsActivity"></activity>
        <activity android:name=".SharedElementAnimationsActivity" />
        <activity android:name=".SceneAnimationActivity" />
        <activity android:name=".CustomViewAnimationsActivity" />
        <activity android:name=".ValueAnimationsActivity" />
        <activity android:name=".ObjectAnimationsActivity" />
        <activity android:name=".ViewAnimationsActivity" />
        <activity android:name=".AnimationDrawableActivity" />
        <activity android:name=".AnimationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>